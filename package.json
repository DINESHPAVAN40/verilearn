{
  "name": "verilog-project-temp",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo no build script defined && exit 0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
